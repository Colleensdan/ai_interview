streamlit==1.42.2
openai==1.63.2
anthropic==0.46.0
# spacy, numpy, and the spaCy language models are intentionally omitted.
# The app uses PhrasePseudonymizer (pure-Python regex) by default.
# To use InterviewPseudonymizer (spaCy NER), install manually:
#   pip install spacy numpy
#   python -m spacy download de_core_news_sm
